// MCP tool implementations (Phase 5)
//
// Tools to implement:
// - scan: trigger filesystem scan
// - list_repos: filter and list repos
// - repo_status: detailed status for one repo
// - freshness: freshness overview
// - search: fuzzy name/path/tag search
// - get_config: read current config
// - run: batch read-only commands
